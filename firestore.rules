rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /cars/{carId} {
      // Auth is now handled by Auth0 on the frontend.
      // Firestore no longer has access to the Auth0 session,
      // so we allow all reads and writes here.
      allow read, write: if true;
    }
  }
}

